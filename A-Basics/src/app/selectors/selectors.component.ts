import {Component} from '@angular/core';

@Component({
  selector: '[app-selectors]',  // others: element, class (.app-selectors)
  template: `
    <div>Attribute selector worked!</div>
  `
})
export class SelectorsComponent {
}
